/**
 * This is the response for interview question.
 * @param para1{number}
 * @param para2{number}
 * @param para3{number}
 * @returns {number}
 */
export let example = (para1, para2, para3) => {
    return para1 + para2 + para3;
}
